# Enterprise-Context-Aware-AI-Agent
A full-stack internal "Backoffice" tool. Users can upload company documents (e.g., HR policies, travel reimbursement rules, legal contracts), and an AI assistant can answer questions based only on those documents, draft emails, and extract structured data (like turning a messy PDF receipt into a clean JSON payload for a database).
